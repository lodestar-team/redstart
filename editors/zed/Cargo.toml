# Standalone workspace — this is a Zed (wasm) extension, not part of the main
# Rust workspace.
[workspace]

[package]
name = "redstart_zed"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.1.0"
