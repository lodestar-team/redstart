{
  "name": "harness-base",
  "version": "0.0.1",
  "private": true,
  "description": "Base project the harness copies per contract: graph-cli + graph-ts + matchstick-as. Generated files are written on top, then codegen/build/test run.",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.38.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
